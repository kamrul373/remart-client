import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center'>
            <button className="btn loading btn-primary btn-wide">loading</button>
        </div>
    );
};

export default Loading;